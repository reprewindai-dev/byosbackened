name: Marketplace Automation Cron

on:
  schedule:
    - cron: "15 */5 * * 1"
    - cron: "0 */6 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  run-marketplace-automation:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger marketplace automation endpoint
        env:
          BACKEND_URL: ${{ secrets.BACKEND_URL || 'https://api.veklom.com' }}
          INTERNAL_API_KEY: ${{ secrets.INTERNAL_API_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${INTERNAL_API_KEY:-}" ]; then
            echo "INTERNAL_API_KEY is not configured; skipping protected marketplace automation."
            exit 0
          fi
          curl -X POST "$BACKEND_URL/api/v1/marketplace/automation/run" \
            -H "Authorization: Bearer $INTERNAL_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{"trigger":"cron","source":"github-actions"}' \
            --retry 3 --retry-delay 10 --retry-all-errors \
            --fail --silent --show-error

      - name: Open incident issue on failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --title "[OPS] Marketplace automation cron failed" \
            --body "Workflow: $GITHUB_WORKFLOW`nRun: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID`nCommit: $GITHUB_SHA`nImpact: marketplace automation may not classify/validate queued listings until this is resolved."
