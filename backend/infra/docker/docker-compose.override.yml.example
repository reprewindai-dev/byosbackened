# Docker Compose Override Example
# Copy to docker-compose.override.yml to customize locally
# This file is gitignored - use for local development overrides

version: '3.8'

# Example: Override API port for local development
# services:
#   api:
#     ports:
#       - "8000:8000"
#     environment:
#       DEBUG: "true"

# Example: Add volume mounts for development
#   api:
#     volumes:
#       - ../../:/app
#     command: uvicorn apps.api.main:app --reload --host 0.0.0.0 --port 8000

# Example: Enable GPU for whisper
#   whisper:
#     deploy:
#       resources:
#         reservations:
#           devices:
#             - driver: nvidia
#               count: 1
#               capabilities: [gpu]
