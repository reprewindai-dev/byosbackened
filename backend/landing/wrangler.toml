# Cloudflare Pages config for veklom.com
# This is a STATIC SITE — no compute, no env vars needed here.
# All secrets / env vars live with the backend (separate deploy on Render/Fly).
#
# Files in this directory deploy as-is:
#   index.html            — landing page
#   logo.svg, favicon.svg — branding assets
#   robots.txt            — crawler directives
#   sitemap.xml           — search-engine sitemap
#   _headers              — security headers (HSTS, CSP, etc.)
#   _redirects            — URL routing rules

name = "veklom"
compatibility_date = "2026-04-25"
pages_build_output_dir = "."
